services:
  - type: worker
    name: Kosem-Bot
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: SESSION_ID
        sync: false
        description: "Apni WhatsApp ki lambi Session ID yahan paste karein (e.g., Kosem!ey...)"